# Python dependencies for Superset automation scripts
# Database migration support
Flask>=2.3.0,<3.0.0
Flask-SQLAlchemy>=3.0.0,<4.0.0
Flask-Migrate>=4.0.0,<5.0.0
psycopg2-binary>=2.9.0,<3.0.0

# For dashboard import (stdlib only, no external deps needed)
# Uses urllib.request, json, pathlib - all in Python standard library
